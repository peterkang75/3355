src/pages/Dashboard.jsx의 handleJoinBooking 함수를 수정해주세요.

현재 이 함수에서는 참가/참가취소 시 프론트엔드에서 직접 apiService.createTransaction()과 apiService.deleteChargeTransaction()을 호출하고 있습니다. 하지만 서버의 PUT /bookings/:id가 participants 변경을 감지하면 자동으로 트랜잭션을 생성/삭제하므로, 프론트에서의 트랜잭션 처리는 중복입니다.

변경 내용:
1. handleJoinBooking 함수 안에서 apiService.createTransaction() 호출 부분을 삭제
2. handleJoinBooking 함수 안에서 apiService.deleteChargeTransaction() 호출 부분을 삭제
3. participationFee 계산과 관련 조건문도 불필요하므로 삭제
4. updateBooking만 호출하면 서버가 알아서 처리합니다

변경 전 (현재 코드, 대략적 구조):
const handleJoinBooking = async (bookingId) => {
  // ... 생략 ...
  if (alreadyJoined) {
    // 참가 취소
    await updateBooking(bookingId, { participants: updatedParticipants });
    if (participationFee > 0) {
      await apiService.deleteChargeTransaction(user.id, bookingId);  // ← 이 줄 삭제
    }
  } else {
    // 참가
    await updateBooking(bookingId, { participants: updatedParticipants });
    if (participationFee > 0) {
      const transactionData = { ... };
      await apiService.createTransaction(transactionData);  // ← 이 줄 삭제
    }
  }
  await refreshBookings();
};

변경 후:
const handleJoinBooking = async (bookingId) => {
  if (joiningBookingId === bookingId) return;
  
  setJoiningBookingId(bookingId);
  try {
    const booking = bookings.find(b => b.id === bookingId);
    const participants = parseParticipants(booking.participants);
    const alreadyJoined = participants.some(p => p.phone === user.phone);
    
    if (alreadyJoined) {
      const updatedParticipants = participants
        .filter(p => p.phone !== user.phone)
        .map(p => JSON.stringify(p));
      
      await updateBooking(bookingId, {
        participants: updatedParticipants
      });
    } else {
      const updatedParticipants = [
        ...participants,
        { name: user.name, nickname: user.nickname, phone: user.phone }
      ].map(p => JSON.stringify(p));
      
      await updateBooking(bookingId, {
        participants: updatedParticipants
      });
    }
    
    await refreshBookings();
  } catch (error) {
    alert('참가/참가 취소 처리에 실패했습니다.');
  } finally {
    setJoiningBookingId(null);
  }
};

핵심:
- participationFee 관련 코드 전체 삭제 (계산, if문, apiService.createTransaction, apiService.deleteChargeTransaction)
- updateBooking과 refreshBookings만 남김
- 서버의 PUT /bookings/:id가 participants 변경을 감지하여 자동으로 참가비 청구/취소를 처리함
- 나머지 함수나 코드는 건드리지 마세요