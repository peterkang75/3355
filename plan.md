# 3355 골프 클럽 앱 - Plan.md

> 최종 수정일: 2026-06-05  
> 전체 이력 및 완료 작업: **Plan_Archive.md** 참조

---

## 현재 상태

| Phase | 내용 | 상태 |
|-------|------|------|
| 1 | 서버 안정화 및 구조 개선 | ✅ 완료 |
| 2 | 프론트 구조 + 모바일 UI + Play 안정성 | ✅ 완료 |
| 3 | 월간 정산 시스템 | ✅ 완료 |
| 4 | 게스트 초대링크 | ✅ 완료 |
| 5 | DB 스키마 정리 | ⏸️ 보류 |
| 6 | Railway 인프라 이전 | ✅ 완료 |
| 7 | 라운딩 사진·동영상 갤러리 | ✅ 완료 (2026-05-26) |
| 8 | 골프장 API + Stableford + 지도 + 게임모드 | 🔄 8A~8C ✅ / 8D~8E ⬜ |
| 9 | 소식 커뮤니티 피드 (사진·반응·댓글·자유글) | ✅ 완료 (2026-05-27) |

**현재 우선순위:** Phase 9(소식 피드) 완료·배포. 다음 후보: ① **앱 전체 이모티콘 정리**(약 200개/30화면, 화면별로, 🥇🥈🥉 등 정보성은 SVG/숫자로 대체 — 사장님 2026-05-27 지시) ② 8-D(Google Maps) / 8-E(2BBB·포썸 점검). 후속: 리더보드 순위계산을 roundingRanking.js 공용함수로 통합.

---

## 미완료 작업

### 회비 차등 청구 — 골프장 멤버 항목별 면제 ✅ (2026-06-05)

> 배경: 6월 정기모임이 스트라스필드 골프클럽 — 클럽 소속 회원은 그린피 면제, 카트비·참가비는 동일 청구. 기존 라운딩은 fee 3개를 전원에게 동일 청구.

**확정 설계:**
- 라운딩의 비용 안내 섹션에 각 항목별 "클럽 멤버 면제" 토글 3개 (그린피/카트비/참가비)
- 토글 ON 시 `member.club === booking.courseName`인 회원에게는 해당 항목 청구액 0
- 비-멤버·club 미입력 회원은 일반 금액 그대로
- description에 면제 사유 자동 메모: "(클럽 멤버: 그린피 면제)" 등
- 모달에 면제 대상 회원 명단 미리보기(N명) — 0명이면 경고로 표시(데이터 정비 유도)

| Step | 작업 | 상태 |
|------|------|------|
| 1 | `Booking.waiveGreenFee/CartFee/MembershipFeeForClubMembers` Boolean 3컬럼 추가 + 운영 DB push | ✅ 2026-06-05 |
| 2 | `bookings.js` — 회원별 청구 계산 헬퍼(`computeMemberChargeForBooking`)로 통합, 참가자 추가·toggle-join·fee 동기화 3곳 일관 처리. waive 토글 변경도 동기화 트리거에 포함 | ✅ 2026-06-05 |
| 3 | `CreateBookingModal`·`HostManageSheet` 비용 섹션에 미니 토글 3개 + 면제 대상 명단 미리보기. 골프장 미선택 시 토글 비활성 | ✅ 2026-06-05 |
| 4 | 빌드(vite)·서버 부팅·문법 검증·props 전달 점검. 실 시나리오(라운딩 생성 후 멤버/비-멤버 참가)는 운영 잔액 영향 회피 위해 사장님 6월 라운딩 생성 시 확인 | ✅ 2026-06-05 |

**구현 메모:**
- 자동 분류 기준은 문자열 완전 일치(`member.club === booking.courseName`). 회원 정보의 소속 클럽 필드가 정확히 입력돼야 함. 현재 운영 DB Strathfield 소속 15명 등록 확인.
- 미래 확장(회원 직접 선택 등 다른 면제 조건)은 후속 — 현재는 "해당 골프장 멤버" 조건 단일.
- 게스트 charge(`category: '게스트 참가비'`)는 회원 면제 규칙 미적용 — 동기화 루프에서 스킵.
- 이미 납부된 charge(payment 트랜잭션 존재)는 fee 동기화에서 스킵 — 기존 안전 로직 유지.

### 공지 자동화 — 홈 배너 (진행중, 2026-05-27)

> 목표: 평소엔 "지난 정기모임 사진·결과" 배너가 **자동**으로 뜨고, 특별공지를 올리면 그게 우선, 종료일 지나면 자동 복귀. 정기모임 중심.

**확정 설계 (접근법 A — 화면에서 계산, 새 서버기능 최소):**
- 배너 우선순위: ① 활성 특별공지(`isFeatured && isActive && (featuredUntil 없음 OR ≥오늘)`, 여럿이면 최신) → ② 없으면 자동 "지난 정기모임" 배너(가장 최근 지난 정기모임) → ③ 둘 다 없으면 미표시.
- 자동 배너: 제목 "○월 정기모임 사진·결과", 누르면 그 라운딩 갤러리로 이동(`reopenGalleryBooking` sessionStorage 재활용 → /booking).
- Post에 **`featuredUntil DateTime?`**(상단 노출 종료일, 선택) 추가.
- 공지 작성/수정 UI에 "상단 노출 종료일"(선택) 추가.

| Step | 작업 | 상태 |
|------|------|------|
| 1 | Post.featuredUntil 추가(db push) + 공지 생성/수정 API가 featuredUntil 수용 | ✅ 2026-05-27 (API 왕복 검증) |
| 2 | 홈 배너 로직: 활성 특별공지(만료 판정) → 없으면 자동 지난 정기모임 배너 + 클릭 시 갤러리 이동 | ✅ 2026-05-27 |
| 3 | 게시판에서 상단공지(featured) 글에 "상단 노출 종료일" 입력(선택) | ✅ 2026-05-27 |

**완료** — 평소 자동 "지난 정기모임 사진·결과" 배너 / 특별공지 우선 / 종료일 지나면 자동 복귀.

### 정산 안정화 (핫픽스 + 후속)

- [x] **2026-05-25 핫픽스** — server.js 시작 시 자동 실행되던 1회성 데이터 정정/삭제 스크립트 제거 (Jacob/동백 5월 거래 삭제·5월 charge 정정·아프로후테 3월 정정). 이 스크립트들이 재시작(배포)마다 재실행되어 5월 정산 마감 후 Jacob·강정환의 정상 회비납부($120씩)를 삭제 → 미수금 오발생. 코드 제거 + 두 납부 복원 완료. (당시 5월 순잔액 $434 = 6월 이월금 일치 검증 — 단 아래 브라이언 정정+도네이션으로 최종 $326으로 재보정됨)
- [ ] **정산 마감 스냅샷화 (구조 개선)** — 현재 "마감"은 `isClosed` 플래그만 저장하고 정산서는 매번 라이브 재계산함. 마감 후 5월 거래가 바뀌면 마감된 정산서가 소급 변경됨. 마감 시 집계 결과를 박제(스냅샷 저장)하여 사후 변경과 분리할 것.
- [x] **2026-05-26 데이터 정정 — 브라이언 크레딧 오류** — 브라이언이 5/1에 크레딧으로 5월 회비 정산($12 잔여) 후, 5/6에 관리자가 일반 납부 $120(설명 "크레딧 처리")을 수동 추가하는 실수로 크레딧이 $132로 잘못 부풀려짐(시스템 오류 아닌 수동 입력 실수, createdBy NULL). 해당 거래 삭제 → 브라이언 크레딧 $12 복구. 이 $120은 5월 마감 전 입력돼 마감값 $434를 부풀렸던 것이라 6월 이월금 $434→$314 보정. 이후 사장님 지시로 **브라이언 잔여 크레딧 $12를 5월 클럽 도네이션 처리**(creditDonation+donation) → 5월 순잔액 $326, 6월 이월금 $314→**$326** 재보정. **최종 5월 정산액=$326.** 정산서 하단에 정정 경위 메모 표시(아래 기능).
- [x] **2026-05-26 기능 — 정산서 정정/메모 표시** — `/report` 응답에 notes 추가 + SettlementReportModal 하단에 메모 카드 렌더링. MonthlySettlement.notes에 저장된 문구를 정산서에 노출(마감 후 정정 경위 안내용).
- [ ] **(개선 검토) 수동 거래추가 시 createdBy 미기록** — `POST /api/transactions`(빠른 입력)는 req.body를 그대로 저장하고 작성자를 강제하지 않아, 수동 입력 거래의 작성자 추적 불가. 감사 위해 서버에서 `req.member.id`로 강제 기록 검토.
- [x] **2026-06-23 환불 개념 정리 + 유령 크레딧 교정** — 발단: 수다쟁이 회원에 원인불명 +$132 크레딧. 진단: '환불'이 코드상 3갈래(① 레거시 expense/환불=잔액영향0 ② 현재버튼 credit/환불=+크레딧 ③ 회원크레딧 credit=+)로 엇갈려 있었고, "낸 돈을 돌려주고 잔액 −"하는 진짜 환불이 부재. 4월 '환불'이 잔액에서 제외되어 청구 없는 입금건(3명: 수다쟁이·테일러·아프로)에 +$132 유령 크레딧 잔류. **개념 재정의**: 환불=회원이 낸 돈 현금 반환(잔액 −), 회원크레딧=안 낸 돈(상금·보상) 크레딧 지급(잔액 +). **Step1(코드)**: `handleClubExpense`에서 '환불'→type=expense·category='회원환불'(차감), '회원크레딧'은 type=credit 유지. 레거시 '환불'(excluded)과 충돌 회피 위해 신규는 '회원환불' 카테고리 사용(18명 무손상). Fees 라벨 '회원환불'/'환불'→"환불" 매핑. **Step2(DB 1회 수동교정)**: 3명의 4/29 환불 기록 category '환불'→'회원환불' 변경(삭제 아님) → 잔액 $0. 안전조건=변경후 잔액 정확히 0인 경우만(18명은 −132라 자동 제외). 백업 보존. **청구취소(이어서 완료)**: 회원 상세 화면 거래내역의 청구(charge) 행에 관리자 전용 '청구취소' 버튼 추가(MemberDetail). 동작=해당 charge 삭제 → 잔액 +금액(미납 제거)·정산서 미수금 제거. 확인창에 "이미 납부된 청구면 +크레딧 남으니 현금반환은 환불로" 안내. 취소기록을 +로 남기는 방식은 정산서가 클럽지출로 오계산하므로 삭제 방식 채택. 이제 "라운딩 취소+환급"=청구취소+환불 세트로 처리 가능.

### Phase 9: 소식 커뮤니티 피드 ✅ (2026-05-27)

> 목표: 지난 라운딩 사진을 한 곳(소식 탭)에 모아 인스타식으로 보고, 반응·댓글로 소통. 사진 동선 강화로 커뮤니티 활성화.

**구현 메모:**
- 하단 네비 **소식 탭** 신설(순서: 홈·소식·라운딩·참가비·더보기), 운영자 **관리는 더보기로** 이동(5개 유지).
- 피드 = **라운딩 자동 게시물**(사진 있는 지난 라운딩, 라운딩당 1개, 제목 자동) + **자유 사진글**(정회원) 혼합, 최신순(새 사진 올리면 위로).
- **업로드 입구 통일**: 우하단 + → "정기모임 사진"(내가 참가한 지난 라운딩 선택 → 기존 라운딩 미디어 업로드 재사용) / "자유 게시물"(글+사진).
- 반응(♡)·댓글: 공용 **다형성 Reaction/Comment 테이블**(targetType booking|feedpost). 게스트는 보기·반응만, 댓글·작성은 정회원. 댓글 삭제는 작성자/운영자.
- 라운딩 **'지난' 탭은 정보 전용**(날짜·참가자·스코어·시상, 사진 없음). 홈 "지난 정기모임" 배너 → 소식 탭.
- 링크: 유튜브 임베드 / 인스타·일반은 링크 카드(서버 OG 미사용).
- 신규 테이블 4개(`FeedPost`/`FeedPostMedia`/`Reaction`/`Comment`) — 운영 `prisma db push`로 추가(기존 데이터 무손상, 컬럼 변경 0). `media.js`(라운딩 업로드) 무수정, 압축 프리미티브만 `feedMedia.js`로 재사용.
- 역검수 수정: 과거 판정 **시드니 시간대** 문자열 비교, 반응/댓글 대상 존재검증(404), 좋아요 더블탭 경합 무시, 라운딩 삭제 시 연결 반응/댓글 정리, 댓글 카운트 실시간 갱신.
- UI: 컬러 이모티콘 미사용(라인 SVG 아이콘). 설계·계획 문서: `docs/superpowers/specs|plans/2026-05-27-*`.
- 후속(2026-06-30): 라운딩 카드 캐러셀 **사진 10장 제한 해제 → 전부 스와이프**(feed.js `slice(0,10)` 제거, signedUrl 로컬서명이라 성능무관). 캐러셀 **좌우 화살표**(끝이면 해당 방향 숨김) 추가 + 사진 8장 초과 시 점 숨기고 카운터만. → 카드 좋아요가 사진 묶음 전체 좋아요로 의미 확보.

**후속(별도):** 앱 전체 이모티콘 정리(약 200개/30화면) — 위 우선순위 참조.

### Phase 5: DB 스키마 정리 ⏸️ 보류

> Railway 운영 안정화 후 진행 예정. 실행 시 잠깐 서비스 중단 가능성 있음.

| # | 작업 | 상태 |
|---|------|------|
| 5-1 | 타입 교정 (isClubMember→Boolean, handicap→Float, amount→Int) | ⏸️ |
| 5-2 | 댓글 테이블 분리 (Post.comments JSON → Comment 모델) | ⏸️ |
| 5-3 | participants 정규화 (String[] → BookingParticipant 테이블) | ⏸️ |
| 5-4 | Booking JSON 필드 구조화 검토 | ⏸️ |

---

### Phase 7: 라운딩 사진·동영상 갤러리 ⬜

> 저장소: **Cloudflare R2** (무료 10GB, 조회 트래픽 무료). 앱 DB에는 R2 객체 key + 메타데이터만 저장.
> 이미지 서빙: **서명 URL(signed URL)** 방식 — 도메인 구입 불필요, R2 무료 트래픽 유지.

**확정 설계:**
- 사진 + 짧은 동영상 모두 지원. 동영상은 서버에서 자동 압축(ffmpeg, 720p) 후 저장.
- 업로드/삭제 권한: 그 라운딩 **참가 회원만**. 개별 사진 삭제는 **올린 사람만**. 월별 대량 정리는 **관리자만**.
- 지난 라운딩 진입: 라운딩 라운지 상단 **예정/지난 토글** → 지난 선택 시 월별 역순 목록(📷 개수 뱃지).
- 라운딩 상세: **정보 / 스코어 / 사진** 3탭.
- 용량 관리: 관리자 "저장소 관리" 화면 — 사용량 막대 + 월별 zip 다운로드(즉석 생성, 미리 저장 안 함) → 삭제.
- 정리된 라운딩: `Booking.photosArchivedAt` 플래그 → 사진 탭에 **"백업 후 정리됨"** 안내.

| Step | 작업 | 상태 |
|------|------|------|
| 1 | Cloudflare R2 가입 + 버킷 생성 + API 토큰 → 로컬 .env + Railway 환경변수 연결 | ✅ 2026-05-25 (버킷 golf-3355-media, 서명URL 방식) |
| 1.5 | 미디어 도구 점검: R2 연결 + ffmpeg-static 스모크 테스트 통과. 썸네일은 기존 jimp 재사용 | ✅ 2026-05-25 |
| 2 | RoundingMedia 테이블 + 업로드/조회/삭제 API + 영상압축·썸네일(jimp/ffmpeg) + 참가자 권한 검증 | ✅ 2026-05-25 (파이프라인 e2e 테스트 통과) |
| 3 | 라운딩 라운지 예정/지난 토글 + 지난 라운딩 월별 역순 목록 (📷 개수 뱃지) | ✅ 2026-05-25 |
| 4 | 상세 시트 "사진·영상" 버튼 → 전체화면 갤러리 (그리드·확대·다운로드·단일삭제). HTTP e2e 검증 통과 | ✅ 2026-05-25 |
| 5a | 저장소 관리(관리자): 사용량 막대 + 월별 zip 백업 다운로드 | ✅ 2026-05-26 (zip 무결성 검증, archiver v7) |
| 5b | 월별 삭제 + 2단계 확인 + photosArchivedAt + 카드 "백업 후 정리됨" 표시 | ✅ 2026-05-26 (빈 달 임시데이터로 삭제 e2e 검증) |
| 6 | 업로드 개수 제한 제거(사장님 요청, 200MB/파일 안전장치만) + 관리자 페이지 상단 80% 사용량 알림 배너 | ✅ 2026-05-26 |

**기술 결정/주의 (역검수):**
- 이미지 서빙 = 서명 URL (도메인 불필요, 갤러리 로드 시 서버가 단기 URL 발급).
- 영상 압축은 시간 소요 → 업로드는 "처리 중" 비동기 상태로, 모바일 타임아웃 회피.
- HEIC(아이폰 기본) → JPEG 변환 필수, EXIF 회전 보정.
- "백업 후 정리됨" = (b) RoundingMedia 행 삭제 + Booking.photosArchivedAt 세팅 방식.

**완료 기준:**
- [ ] 라운딩별 사진/동영상 업로드(압축)·조회·다운로드·단일 삭제 동작
- [ ] 예정/지난 토글 + 라운딩 상세 3탭(정보·스코어·사진) 통합 조회
- [ ] 관리자 월별 백업(zip)·정리 + "백업 후 정리됨" 안내 동작
- [ ] 무료 한도 내 운영 (R2 10GB, 서명 URL로 트래픽 무료)

---

### Phase 8: 나머지 작업

> 8-A (골프장 API) ✅, 8-B (Course 모델 확장) ✅, 8-C (Stableford + Pickup) ✅ 완료

#### 8-D: 라운딩 정보에 골프장 위치 연동

> 2026-04-16에 라운딩 카드 "지도보기" 버튼(Google Maps 좌표/주소)이 일부 추가됨.

| # | 작업 | 상태 |
|---|------|------|
| 8D-1 | 라운딩 카드/상세에 골프장 주소 텍스트 표시 | ⬜ |
| 8D-2 | Google Maps 연결 버튼 ("지도보기") | 🔄 부분완료 |
| 8D-3 | Google Maps 길찾기 바로가기 버튼 | ⬜ |

#### 8-E: 기존 게임 모드 점검

| # | 작업 | 상태 |
|---|------|------|
| 8E-1 | 2BBB(투볼베스트볼) 전체 플로우 테스트 | ⬜ |
| 8E-2 | 포썸(Foursome) 전체 플로우 테스트 | ⬜ |

#### 기타 잔여 항목

- [x] **2026-05-27 컴페티션 인원 무제한** — 컴페티션 라운딩은 정원(maxMembers) 제한 없이 가입 가능하도록 변경. 백엔드 가입 게이트에서 `type==='컴페티션'`이면 정원 체크 생략 + 표시도 "/정원" 제거(getStatusBadge·BookingBottomSheet·RoundingListV2). 정원 설정 UI는 4/28부터 컴페티션에서 숨김 유지(생성·관리). type 기반이라 데이터 변경 불필요.
- [ ] **기존 골프장 SI 재조회** — bluegolf 스크래핑 또는 수동 입력 (8B 완료 당시 기존 데이터 미처리)
- [ ] **8C-4: 개인 스코어 히스토리에 Stableford 총점 표시** (Play/Leaderboard는 완료, 히스토리 미반영)

**Phase 8 완료 기준:**
- [ ] 골프장 위치 정보 → 라운딩 상세에서 Google Maps 연결 동작
- [ ] 2BBB, 포썸 모드 정상 동작 확인

---

### Phase 9: 엠브로스(Ambrose) 경기 방식 추가

> 4인 1팀이 공동 점수 하나를 기록하는 팀 스크램블 방식. 기존 스트로크/포썸/2BB 무변경, `gradeSettings.mode='ambrose'`로 분기.
> 확정 규칙: **그로스**(핸디 미적용) · **팀 공용 카드**(마커 없이 팀원 누구나 입력) · **조 = 팀**(기존 조편성 재사용).
> 설계: 팀 점수를 팀원 전원 `Score` 행에 복제(포썸 패턴 확장) + `gameMode='ambrose'` 태그. 리더보드는 팀 그로스 오름차순.

| Step | 작업 | 상태 |
|------|------|------|
| 1 | 경기방식 선택 UI에 엠브로스 추가 (BookingForm·RoundingManagement·HostManageSheet) | ✅ 2026-07-21 |
| 2 | 조편성 = 4인 단일팀 (TeamFormation 기본 그리드 분기 그대로 사용, 무변경) | ✅ 2026-07-21 |
| 3 | Play 팀 공용 카드 흐름 (우리팀 확인 → 1열 스코어카드 → 마커/검증 스킵 → 팀 저장, 저장지점 3곳 + 실시간 소켓 수신) | ✅ 2026-07-21 |
| 4 | 서버: 같은 조 전원에게 팀 점수 원자적 복제 (`prisma.$transaction`) | ✅ 2026-07-21 |
| 5 | 리더보드 엠브로스 팀 랭킹 (결정적 대표행, 그로스 오름차순) | ✅ 2026-07-21 |
| 6 | 배지(BookingListCard/Booking) + 개인통계 가드 (MyPage·MemberDetail 핸디캡·라운드수·베스트 제외, 최근기록 "엠브로스(팀)" 라벨) | ✅ 2026-07-21 |

**역검수 반영(advisor):**
- 동시 입력 → 팀 전원 행 원자적 트랜잭션 + 대표행 결정적 선택(최저 슬롯) + 스코어카드 실시간 소켓 갱신(편집 중 덮어쓰기 방지 가드).
- 개인 통계 오염 → 엠브로스 그로스는 개인 핸디캡/평균/라운드수 계산에서 제외.
- Play.jsx 중복 저장지점(자동저장·점수확인·라운드종료) 전부에 엠브로스 분기 반영.

**전제/주의:**
- 포썸과 동일하게 **조편성 필수**(booking.teams 없으면 팀 묶기 불가 → "조편성 안됨" 안내).
- 팀에 게스트(비회원) 포함 시 게스트 행은 복제 생략, 팀 대표점수는 실회원 행으로 표시 → 팀 순위엔 영향 없음.
- 3인 조는 순수 그로스라 4인 조 대비 불리(스크램블 특성). 운영 시 조 인원 4명 권장.

**완료 기준:**
- [ ] 실제 앱에서 엠브로스 라운딩 생성→조편성→팀원 A 입력→팀원 B 공유·수정→팀 리더보드→개인핸디 미오염 e2e 확인
- [ ] 기존 스트로크/포썸 회귀 없음 확인

---

## 버그 수정 — 소식 사진 업로드 라운딩 목록에 "당일 라운딩" 누락 ✅ 2026-07-26

**증상:** 7월 정기라운딩(2026-07-26) 당일 저녁, 참가 회원(구태웅/싱글워너비)이 소식 → 사진 올리기에서 라운딩 목록을 열었을 때 7월 정기모임이 뜨지 않음 (6월·3월만 표시).

**원인:** `RoundPhotoUploader.jsx`의 "지난 라운딩" 필터가 `b.date < today`(시드니 기준) 로 **오늘 날짜를 제외** → 라운딩 당일에는 그 라운딩을 선택할 수 없었음. 다음날이 되어야 목록에 등장.

**수정:** `b.date <= today` (오늘 포함). 서버 측 업로드 날짜 제한은 없어 클라이언트 한 줄 수정으로 해결.

**후속 검토 대상(미결):**
- [x] 참가자 매칭이 `phone` 스냅샷 기준 → 부킹 생성 후 회원이 전화번호를 바꾸면 해당 라운딩이 영구히 사라짐. → **회원 ID 우선 매칭 보강 완료 (2026-07-26)**: `p.memberId === user.id || p.id === user.id || p.phone === myPhone`.
  - 커버리지 한계: 참가자 엔트리 중 회원 ID를 가진 비율은 **전체 21% (497건 중 105건)**, 최근 2026-07도 64%. 오래된 부킹은 여전히 전화번호 매칭에만 의존.
  - 근본 원인: **참가자 추가 경로 일부가 `memberId`를 안 싣는다** — `RoundingListV2.jsx:369/395/421/424`, `server/routes/bookings.js:632`. 여기에 `memberId` 추가하면 앞으로 생성되는 부킹은 100% 커버. (미승인)
  - 과거 데이터 소급 재작성(전화번호 변경 시 booking.participants/teams/Score 전파)은 **하지 않기로 함** — 정산이 라이브 재계산 구조라 과거 금액이 조용히 바뀔 위험.
- [ ] 동일 패턴 `MyPage.jsx:514` ("내가 참가한 라운딩" 목록)도 전화번호 단독 매칭 → 같이 보강할지 확인 필요.
- [ ] 참가자 추가 경로에 `memberId` 항상 포함시킬지 확인 필요 (위 근본 원인).
- ~~시트 제목은 "어느 라운딩 사진인가요?"인데 필터는 `type === '정기모임'`만 통과 → 클럽 컴페티션 사진 업로드 불가.~~ → **의도된 동작 확정 (사장님 확인, 2026-07-26). 컴페티션 사진은 업로드 대상 아님.**

---

## 2026년 7월 정산 오류 조사 (2026-08-01)

### 1. 브라이언 $253 회비납부 — 원인 확정, 정정 대기
**증상:** 정산서에 브라이언 회원이 클럽에 $253을 납부한 것으로 표시. 실제로는 마케팅 물품($190)을 대납하고 환급받은 건.

**근본 원인:** `server/utils/balance.js:42-44` — 회원 잔액 계산식이
`잔액 = payment + credit − charge − expense` 인데, **memberId가 붙은 `expense`를 그 회원의 빚으로 차감**한다.
지출 입력 시 "집행자" 의미로 회원을 고르면 부호가 정반대가 됨.

**증거:** $253 납부 입력(7/25 01:03) 직전 브라이언 잔액이 정확히 **−253** (7/9 라운딩 청구 −63 + 7/25 마케팅 지출 −190). 앱이 "미납 $253"으로 표시 → 총무가 그대로 납부 처리. DB 전체에서 이런 케이스는 이 1건뿐.

**정정 2건 — ✅ 완료 (2026-08-01):**
- [x] 브라이언 회비납부 $253 → $63
- [x] 마케팅 지출 $190의 회원 지정 해제 (영수증 1장 보존 확인). 앱에서는 불가해서 DB 직접 처리 — `transactions.js:709` PUT이 memberId를 받지 않음.
- 브라이언 잔액 $0 확인.

**재발 방지 제안 (미승인):** 지출 입력 폼의 "회원 (선택)" 드롭다운이 무설명 → 크레딧 차감 외 지출에서는 숨기거나 경고. 잔액 계산식은 건드리지 않는 쪽이 안전.

### 2. 카트비 $225 현금 수입 누락 — ✅ 처리 완료
골프장 지출 $999 = 그린피 18명 × $43 = $774 + 카트비 $225. 카트비는 당일 현금 수납했으나 장부 미기록.
→ 2026-07-26 / 참가비 / $225 / "7월 정기라운딩 카트비 (당일 현금 수납)" 수입 생성, 7월 정기라운딩 부킹에 연결.

### 3. 아리수 7월 청구 — 조치 불필요 (이중청구 위험)
청구는 이미 존재. 단 `크레딧 자동 차감` 형태(7/9 $63)라 일반 청구 목록에 안 보임.
자금 흐름: 6/8에 실제 청구액 $30보다 많은 $110 현금 납부 → $80 크레딧 발생 → 7월 $63이 그 크레딧에서 차감.
즉 **현금은 6월에 이미 수령**. 신규 청구+납부 생성 시 $63 이중 계상.
현재 아리수 잔액 = **$117 크레딧** (클럽이 보관 중인 아리수 님 돈).

### 4. 7월 순액
| 단계 | 수입 | 지출 | 순액 |
|---|---|---|---|
| 조사 시작 시점 | $1,631 | $1,614 | +$17 (오류 2개가 상쇄된 값) |
| 카트비 $225 반영 (현재) | $1,856 | $1,614 | +$242 |
| 브라이언 정정까지 하면 | $1,666 | $1,614 | **+$52** |

### 5. 도네이션이 개인 크레딧으로 둔갑 — ✅ 정정 완료 (2026-08-01)
**원인:** 도네이션 기록 경로가 두 갈래.
- 관리 화면 도네이션 기능 → `type='donation'` → 개인 잔액 영향 없음 (정상, 21건)
- **정산 화면 "수입 추가" → 항목 '도네이션'** → `type='payment'` 로 저장 → **그 회원 크레딧이 됨** (3건)

`Settlement.jsx:104`이 수입 모드를 무조건 `type:'payment'`로 보내는 것이 원인.
크레딧이 생기면 `issueRoundingChargeForMember`가 다음 라운딩 청구 시 `currentBalance > 0`이면 자동 차감 → **기부금으로 본인 참가비를 내게 됨.**

**정정 내역:**
| 회원 | 거래 | 조치 | 잔액 |
|---|---|---|---|
| 중꺽마 | 7/25 도네이션 $230 | type payment→donation | $230 → **$0** |
| 아리수 | 5/10 도네이션 $200 | type payment→donation | |
| 아리수 | 5월 참가비 납부 $120 | 신규 생성 + **5/10 도네이션 $200 → $80으로 감액** (그 입금에서 참가비가 충당됐으므로) | |
| 아리수 | 7/9 크레딧 자동 차감 쌍 $63 | 정상 청구 $63 + 회비납부 $63 으로 정정 (가짜 크레딧 기반이었음) | |
| 아리수 | 7/12 도네이션 $100 | type payment→donation ($100 전액 유지) | $117 → **$0** |
| 아리수 | 7/9 참가비 $63 | 크레딧 자동 차감 → 정상 청구 + **회비납부**(별도 입금 확인, 입금처리 누락분) | |

**아리수 님 최종 확정 (2026-08-01 사장님 확인):**
- 5월: 입금 $200 = 참가비 $120 + 도네이션 $80 (한 번에 입금)
- 7월: 도네이션 $100 **+** 참가비 $63 **별도 입금** — 입금처리가 누락돼 있었음
- 전체 건넨 현금 = $1,215 − 4월 환불 $132 = **$1,083**. 잔액 $0.

**재발 방지 (코드):** `transactions.js` POST — `category==='도네이션' && type==='payment'` → `type='donation'` 자동 보정.
기존 `'회원 크레딧'` 자동 보정과 같은 자리. 프론트 3개 화면 수정 없이 모든 입력 경로 차단.

### 6. 이월금 — ⚠️ 2026-08 한 건만 수정 필요 (사장님 조작)
아리수 정정을 도네이션 감액 방식으로 처리한 결과 **5월·6월은 수입 변동 0** → 이월금 그대로.
7월만 순 +$35 변동(카트비 +225 / 브라이언 −190).

| 달 | 이월 + 수입 − 지출 = 순액 | 다음달 이월금 |
|---|---|---|
| 2026-05 | $191 + $1,412 − $1,277 = $326 | $326 ✓ 변경 없음 |
| 2026-06 | $326 + $1,220 − $1,153 = $393 | $393 ✓ 변경 없음 |
| 2026-07 | $393 + $1,729 − $1,614 = $508 | $410 → **$508** |

**갱신 방법(권장):** 7월은 이미 마감 상태(closedAt 2026-07-31). 참가비 탭 → 7월 → **마감 취소** → **7월 마감 및 이월** 재실행하면 `close` 엔드포인트가 8월 이월금을 자동으로 $508로 기록. 8월 이월금을 손으로 입력하는 것보다 안전.

**7월 손익 $115** = 클럽회비 400 − 마케팅 190 − 상품 195 + 순 도네이션 100(중꺽마 230은 도네이션 상품 230과 상쇄, 아리수 100은 순수 기부).

**주의:** 2026-03 말 이월금이 $316 → $191로 수동 하향($125) 조정된 이력 존재. 사장님 지시로 **그대로 둠**(2026-08-01). 원인 미규명이므로 원장 누적($753)과 저장 이월금 체계는 계속 $125 차이가 남.

### 7. 미결 (사장님 확인 대기)
- [x] **게스트 참가비 청구에 회비 누락 — ✅ 완료 (2026-08-01)**
  - 원인: 게스트 청구가 `greenFee + cartFee`만 계산, `membershipFee` 제외. 3곳(`bookings.js:478`, `guest.js:187`, `guest.js:281`)에 중복 구현.
  - 코드: `server/utils/fees.js`에 `computeGuestChargeForBooking()` 단일 소스 신설(그린피+카트비+회비), 3곳 모두 교체.
  - 데이터: 7/26 게스트 6명 청구 $43 → $63 정정 → 각 잔액 $0. 청구 금액 변경은 수입·지출 집계에 영향 없음(검증 완료, 7월 손익 $115 불변).
  - **6/28 게스트 2명(송지연·케니)은 손대지 않음** — 청구 $80 / 실제 납부도 $80. 정상 청구액은 $110이나 올리면 받지도 못한 $30씩 미납이 발생. 사장님 판단 필요.
- ~~6월 게스트 회비 미수~~ → **사장님 확인: 6/28 게스트는 청구·수납 모두 $80이 맞음. 조치 불필요 (2026-08-01).**
- [ ] **붕붕 님 크레딧 $132 — 원인 확정, 정정 대기 (권한 차단으로 미실행)**
  - 원인: 2026-04-15 회비납부 $132(+132) → 4/29 라운딩 취소 환불 $132가 `category='환불'`로 기록됨.
    `balance.js`의 `EXCLUDED_EXPENSE_CATEGORIES = ['환불']` 때문에 **환불이 잔액에서 차감되지 않음** → 유령 크레딧 $132.
  - 붕붕 님은 4월 청구가 없음(선납 후 라운딩 취소). 현금 기준 net 0인데 장부는 +$132.
  - 2026-06-23 유령 크레딧 교정 때 3명(수다쟁이·테일러·아프로)은 처리됐으나, 당시 붕붕 님 잔액이 $82라 "정정 후 잔액 0" 안전조건에 걸려 제외됨. 이후 6/23 납부 $50으로 $132가 되어 지금은 조건 충족.
  - 조치안: 해당 환불 1건의 category `'환불'` → `'회원환불'` (2026-06-23에 정립된 방식과 동일). 정정 후 잔액 정확히 $0.
  - 정산 영향 없음 — settlement.js는 `'환불'`·`'회원환불'` 둘 다 지출로 집계.
- [ ] `balance.js` 환불 제외 카테고리가 `['환불']` 화이트리스트 — 4/29 테일러 님만 카테고리가 `'회원환불'`이라 잔액에서 $132 차감 중. 동일 성격 6건과 불일치. (수정 시 잔액 이동하므로 before/after diff 선행 필수)
